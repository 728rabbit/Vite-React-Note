## 🎯 今日目標

1.  完成專案最終檢查與優化
    
2.  Build 專案，準備部署
    
3.  部署到 **Netlify / Vercel / GitHub Pages**
    
4.  確認功能、UI、資料儲存一切正常
    

----------

## 🧩 一、專案收尾檢查

在部署前，先檢查：

1.  **功能完整**
    
    -   新增 / 刪除 / 勾選完成 ✅
        
    -   localStorage 或 API 資料持久化 ✅
        
    -   主題切換（亮 / 暗） ✅
        
2.  **UI 完整**
    
    -   間距、排版、顏色、按鈕 hover ✅
        
    -   手機 / 桌面響應式 ✅
        
3.  **程式結構整齊**
    
    -   組件拆分合理
        
    -   重複邏輯抽成 Hook
        
    -   使用 Context / Reducer 管理狀態
        

----------

## 🧩 二、打包 Build

在 Vite 專案中：

`# 產生生產環境 build npm run build` 

會生成一個 `dist/` 資料夾，裡面就是可以上線的靜態檔案。

✅ 確認本地測試：

`npm install -g serve
serve -s dist` 

開瀏覽器打開 localhost，可以模擬正式環境。

----------

## 🧩 三、部署選擇

### 1️⃣ **Vercel（推薦 React 專案）**

1.  登入 Vercel
    
2.  連接 GitHub 倉庫
    
3.  選擇專案，Vercel 會自動 detect React / Vite
    
4.  點擊 Deploy，就會自動 build & 上線
    

✅ 完成後會拿到一個 URL，例如：

`https://my-todo-app.vercel.app` 

----------

### 2️⃣ **Netlify**

1.  登入 Netlify
    
2.  連接 GitHub 專案
    
3.  Build command: `npm run build`
    
4.  Publish directory: `dist`
    
5.  點擊 Deploy
    

----------

### 3️⃣ **GitHub Pages**

1.  將 `dist` 資料夾推到 GitHub repository
    
2.  設定 Pages source 指向 `dist`
    
3.  URL 會是：
    

`https://你的帳號.github.io/專案名稱` 

> Vite + React 用 GitHub Pages 需要注意 `vite.config.js` 中的 `base` 設定：

`export  default  defineConfig({ base: "/專案名稱/",
  ...
});` 

----------

## 🧩 四、部署後檢查

1.  **功能**：新增、刪除、完成勾選、資料保存 ✅
    
2.  **UI**：間距、顏色、響應式 ✅
    
3.  **路由**：確保 React Router 路由在刷新頁面時不出錯 ✅
    
    -   Netlify / Vercel 會自動處理
        
    -   GitHub Pages 需要加 `HashRouter`
        

----------

## 🧩 五、進一步優化（可選）

-   壓縮圖片 / 優化資源
    
-   使用 React Lazy Loading 加速頁面
    
-   增加 favicon、SEO meta
    
-   用 GitHub Actions 自動部署
