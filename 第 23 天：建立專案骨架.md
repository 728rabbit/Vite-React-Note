## 🎯 今日目標

1.  建立 React 專案（Vite / CRA 可選）
    
2.  設置 **頁面結構與路由**
    
3.  建立核心組件骨架（Header、Footer、主要容器）
    
4.  初始化狀態管理骨架（Context / useReducer）
    

----------

## 🧩 一、建立專案

    npm create vite@latest my-project
    cd my-project
    npm install
    npm run dev


專案結構建議：

    src/
    ├─ main.jsx
    ├─ App.jsx
    ├─ pages/
    │   ├─ Home.jsx
    │   ├─ Todo.jsx
    │   └─ About.jsx
    ├─ components/
    │   ├─ Header.jsx
    │   ├─ Footer.jsx
    │   └─ TodoInput.jsx
    ├─ context/
    │   └─ TodoContext.jsx
    ├─ reducers/
    │   └─ todoReducer.jsx

----------

## 🧩 二、路由設定（React Router）

    npm install react-router-dom

    // main.jsx
    import React from "react";
    import ReactDOM from "react-dom/client";
    import { BrowserRouter } from "react-router-dom";
    import App from "./App";
    
    ReactDOM.createRoot(document.getElementById("root")).render(
      <BrowserRouter>
        <App />
      </BrowserRouter>
    );

    // App.jsx
    import { Routes, Route, Link } from "react-router-dom";
    import Home from "./pages/Home";
    import Todo from "./pages/Todo";
    import About from "./pages/About";
    import Header from "./components/Header";
    import Footer from "./components/Footer";
    
    export default function App() {
      return (
        <div>
          <Header />
          <Routes>
            <Route path="/" element={<Home />} />
            <Route path="/todo" element={<Todo />} />
            <Route path="/about" element={<About />} />
          </Routes>
          <Footer />
        </div>
      );
    }

----------

## 🧩 三、建立組件骨架

### Header.jsx

    import { Link } from "react-router-dom";
    
    export default function Header() {
      return (
        <header>
          <nav>
            <Link to="/">首頁</Link> | <Link to="/todo">Todo List</Link> |{" "}
            <Link to="/about">關於</Link>
          </nav>
        </header>
      );
    }



### Footer.jsx

    export default function Footer() {
      return <footer>© 2025 My React Project</footer>;
    }


----------

### Pages 骨架（Home / Todo / About）

    // Home.jsx
    export default function Home() {
      return <h1>歡迎來到首頁</h1>;
    }
    
    // About.jsx
    export default function About() {
      return <h1>關於我們</h1>;
    }
    
    // Todo.jsx
    export default function Todo() {
      return <h1>Todo List 頁面骨架</h1>;
    }

----------

## 🧩 四、初始化 Context / Reducer 骨架

    // context/TodoContext.jsx
    import { createContext, useReducer } from "react";
    import { todoReducer } from "../reducers/todoReducer";
    
    export const TodoContext = createContext();
    
    export default function TodoProvider({ children }) {
      const [todos, dispatch] = useReducer(todoReducer, []);
      return (
        <TodoContext.Provider value={{ todos, dispatch }}>
          {children}
        </TodoContext.Provider>
      );
    }

    // reducers/todoReducer.jsx
    export function todoReducer(state, action) {
      switch (action.type) {
        default:
          return state;
      }
    }
