## 🧩 什麼是「組件」？

在 React 裡，一個「組件（Component）」就是一個可以重複使用的小功能或區塊。

比方說：

-   一個按鈕是一個組件
  
-   一個導航列（Navbar）是一個組件
    
-   整個頁面由很多組件組成
    
組件就像「樂高積木」，你可以把它們拼成更大的畫面。


## ⚙️ 建立第一個 React 組件

### 1️⃣ 在 `src/` 資料夾內新建一個檔案：

`src/units/Hello.jsx`

### 2️⃣ 寫入以下內容：

    function Hello() { 
    	return ( 
	    	<div> 
		    	<h2>Hello, React 組件！</h2>
		    	<p>這是一個自訂組件的範例。</p>
	    	</div>
    	);
    } 
    export default Hello

## 🧠 在 `App.jsx` 裡使用組件

打開 `src/App.jsx`，修改成這樣：

    import Hello from  './units/Hello'  // ⬅️ 匯入剛剛的組件  
    function  App() { 
	    return ( 
		    <div> 
			    <h1>第 3 天：React 組件</h1> 
			    <Hello /> 
			    <Hello /> 
		    </div>
      );
    } 
    export default App

儲存後，瀏覽器會顯示兩個 `Hello` 區塊。  
這就代表你成功建立並重複使用了一個組件！✅

----------

## 🧩 組件的「Props」傳值

組件可以像 HTML 標籤一樣傳遞參數。  
修改 `Hello.jsx`：

    function Hello(props) { 
    	return ( 
	    	<div>
		    	<h2>Hello, {props.name}！</h2> 
		    	<p>歡迎來到 React 世界 🌍</p> 
	    	</div>
      )
    } 
    export default Hello

再修改 `App.jsx`：

    import Hello from  './units/Hello'  // ⬅️ 匯入剛剛的組件  
    function App() { 
	    return ( 
		    <div>
		      <h1>第 3 天：Props 傳值練習</h1>
		      <Hello name="Ken" />
		      <Hello name="Mary" />
		      <Hello name="Jack" />
		    </div>
      );
    } 
    export default App 

結果會顯示：

    Hello, Ken！
    Hello, Mary！
    Hello, Jack！
