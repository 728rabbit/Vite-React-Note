## 🧠 一、什麼是條件渲染？

-   條件渲染就是 **依據狀態（state）來決定畫面要顯示什麼**。
    
-   React 提供幾種寫法：
    
    1.  三元運算符 `? :`
        
    2.  短路運算符 `&&`
        
    3.  `if` 判斷（通常放在 render 之前）
        

----------

## 🧩 二、練習 1：登入 / 登出範例

    import { useState } from 'react';
    
    function App() {
      const [isLoggedIn, setIsLoggedIn] = useState(false);
    
      function toggleLogin() {
        setIsLoggedIn(!isLoggedIn);
      }
    
      return (
        <div>
          <h1>條件渲染練習</h1>
          {isLoggedIn ? (
            <p>歡迎回來，Ken！</p>
          ) : (
            <p>請先登入</p>
          )}
          <button onClick={toggleLogin}>
            {isLoggedIn ? '登出' : '登入'}
          </button>
        </div>
      );
    }
    
    export default App;


-   說明：
    
    -   `isLoggedIn ? <p>歡迎回來</p> : <p>請先登入</p>` → 三元運算符決定文字
        
    -   按鈕文字也根據狀態切換
        
    -   點擊按鈕，畫面和文字都會變
        

----------

## 🧩 三、練習 2：短路運算符

    import { useState } from 'react';
    
    function App() {
      const [showMessage, setShowMessage] = useState(false);
    
      return (
        <div>
          <h1>短路運算符練習</h1>
          <button onClick={() => setShowMessage(!showMessage)}>
            顯示/隱藏訊息
          </button>
          {showMessage && <p>這是一個短路運算符顯示的訊息！</p>}
        </div>
      );
    }
    
    export default App;


-   說明：
    
    -   `showMessage && <p>訊息</p>` → 當 `showMessage` 為 `true` 才顯示 `<p>`
        
    -   很適合「是否顯示某個元件」的情況
